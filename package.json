{
  "private": true,
  "scripts": {
    "dev": "npm --prefix absent-antimatter run dev",
    "build": "npm --prefix absent-antimatter run build",
    "preview": "npm --prefix absent-antimatter run preview"
  }
}
